package com.hillel.tour.agency.api.validator.impl;

import com.hillel.tour.agency.api.dto.TourCreationDto;
import com.hillel.tour.agency.api.validator.Validator;

public class TourValidator implements Validator<TourCreationDto> {
    @Override
    public void validate(TourCreationDto entity) {
        //validation
    }
}
